# AGENTS.md

## Cursor Cloud specific instructions

This repository (`Cursor_Test`) is currently an empty/blank repository containing only a `README.md`. There is no application code, no dependency manifests, no build system, and no services to run.

- **No package manager or lockfile** — no `package.json`, `requirements.txt`, `pyproject.toml`, `go.mod`, etc.
- **No build/lint/test commands** — nothing to build, lint, or test.
- **No services** — nothing to start or connect to.

When application code is added in the future, update this file with relevant setup, build, lint, test, and run instructions.
